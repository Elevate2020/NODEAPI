var chai = require('chai'),
    sinon = require('sinon')
    expect = chai.expect;
    chai.should();
let queries = require('../dao/userDao');
const request = require('request');

describe('queries', function(){
    it('should return all rows from table', function(){
        
    })
})